import { ProfileClient } from '@/components/profile/ProfileClient';

export const metadata = {
  title: 'User Profile & Settings | Velocis EKMS',
  description: 'Manage personal enterprise credentials, department focus, and authentication security in Velocis EKMS.',
};

export default function ProfilePage() {
  return <ProfileClient />;
}
